pre-commit:
  parallel: true
  jobs:
    - name: typecheck
      run: npm run typecheck
    - name: biome
      run: npx biome check --write {staged_files}
      glob: "*.{ts,js,json}"
      stage_fixed: true
